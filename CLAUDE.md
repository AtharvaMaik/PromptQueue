---
name: promptqueue
description: Schedule prompts to run later through this repo's dependency-free Python queue. Use when the user says their AI limits reset at a time, asks to send/paste a prompt later, or asks to queue a prompt for Claude, Codex, ChatGPT, Gemini, Copilot, Cursor, Antigravity, or another AI tool.
---

# PromptQueue

Follow `AGENTS.md` for scheduling prompts with this repo.

When no target is named and the user is talking to Claude, use `claude`. Use `claude-code` only when the user asks for the Claude CLI.
